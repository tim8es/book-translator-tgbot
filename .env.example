# No plain-text Telegram bot token is required by the workflow.
# Store the bot token only in an n8n Telegram API credential.
#
# Configure ADMIN_USER_ID and ADMIN_CHAT_ID locally inside the workflow node
# "Admin Config" after import. Do not commit real personal IDs if the repository
# is public.
